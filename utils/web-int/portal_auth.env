# Shared workshop portal login (HTTP Basic Auth).
# Shipped in the repo so lab VMs do not need /fabric/credentials.env changes.
#
# PORTAL_AUTH=enabled  — require HTTP Basic Auth (default if unset)
# PORTAL_AUTH=disabled — no portal password; the site is open
PORTAL_AUTH=enabled
PORTAL_USER=admin
PORTAL_PASSWORD=Fortinet123#
